# Dependencies
node_modules/

# OS cruft
.DS_Store

# Build byproducts
*.map

# Release archives (built, not tracked)
*.zip
*.tgz
# ...except the bundled sample-data seed, which is a shipped theme asset (not a build byproduct)
!zorderz/sample-data/*.zip

# Vendored dependencies
/vendor/

# PHPUnit cache
.phpunit.result.cache
.phpunit.cache/

# Vendored apps bundle + build output (produced by build.sh, not tracked)
/zorderz/bundled/zorderz-apps/
/dist/

# Git bundles (produced for hand-off, not tracked)
*.bundle
